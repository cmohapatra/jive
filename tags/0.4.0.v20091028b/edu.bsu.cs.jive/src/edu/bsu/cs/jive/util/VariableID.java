package edu.bsu.cs.jive.util;

/**
 * Identifies a variable within a contour.
 * This cannot be done with a simple string since a single contour may
 * contain multiple homonymous variables within (separate) parallel
 * contexts.
 *
 * @author pvg
 */
public interface VariableID {

}
